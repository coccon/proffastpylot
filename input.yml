instrument_number: SN036

# path to profast directory, automaticaly determined
root_path: default

# default: ${root_path}\auxil\map-files\${site}
map_path: default

#default ${root_path}\auxil\log-files\${site}
pt_path: default

coordinates:
  longitude: 20
  latitude: 20
  altitude: 0.3  # in km

# optional comment included in bin-files, False or string
note: False

# parameters to generate template files ...

# site:
# prep_tempfile: template_preprocess4.inp
# pcxs10_tempfile = template_pcxs10.inp
# inv_tempfile = template_invers10.inp
# pt_tempfile = template_pT_intraday.inp
